
      <% if current_college %> 
            
            <%if current_college.following?(company)%>
                  <%= form_for(current_college.relationships.find_by(company_id: company.id), html: { method: :delete },remote: true) do |f|%>
                        <%= hidden_field_tag :company_id, company.id%>
                        <%= f.submit 'アンフォローする', class:'btn btn-danger btn-sm'%>
                  <% end %>
            <% else %>
                  <%= form_for(current_college.relationships.build,remote: true) do |f|%>
                        <%= hidden_field_tag :company_id, company.id%>
                        <%= f.submit 'フォローする', class:'btn btn-primary btn-sm'%>
                  <% end %>
            <% end %>
      <% else %>
            <%if current_company.following?(college)%>
                  <%= form_for(current_company.relationships.find_by(college_id: college.id), html: { method: :delete },remote: true) do |f|%>
                        <%= hidden_field_tag :college_id, college.id%>
                        <%= f.submit 'アンフォローする', class:'btn btn-danger btn-sm'%>
                  <% end %>
            <% else %>
                  <%= form_for(current_company.relationships.build,remote: true) do |f|%>
                        <%= hidden_field_tag :college_id, college.id%>
                        <%= f.submit 'フォローする', class:'btn btn-primary btn-sm'%>
                  <% end %>
            <% end %>
      <% end %>
