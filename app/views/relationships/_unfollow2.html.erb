<%= form_for(current_company.relationships.find_by(college_id: college.id), html: { method: :delete },remote: true) do |f|%>
                        <%= hidden_field_tag :college_id, college.id%>
                        <%= f.submit 'アンフォローする', class:'btn btn-danger btn-sm'%>
                  <% end %>